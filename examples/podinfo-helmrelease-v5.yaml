apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: default
spec:
  interval: 10m
  chart:
    spec:
      chart: podinfo
      version: '5.0.x'
      sourceRef:
        kind: HelmRepository
        name: podinfo
        namespace: default
      interval: 10m
  values:
    replicaCount: 2
